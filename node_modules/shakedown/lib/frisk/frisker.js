var receive = function(evt){
    var data = evt.data;

    if (data.cmd == 'watch'){

    }
    //postMessage('Hi');
}

var isBrowser = typeof window != 'undefined';
if (isBrowser) onmessage = receive;
else global.onmessage = receive;
