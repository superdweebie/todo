export default function (bundles, query, model, roles) {
        console.log(bundles);
        console.log(query);
        console.log(model);
    return (callback) => {
        callback('asdf');
    }
}
