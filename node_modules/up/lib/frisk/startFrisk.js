import createWorker from 'up/createWorker';

export default function frisk(friskConfig){
    var frisk = createWorker('up/frisk/frisk');
    frisk.postMessage({cmd: 'config', message: friskConfig});
    return frisk;
}
