export default (connectionString) => {
    return (callback) => {
        System.require('mongodb').MongoClient.connect(connectionString, (err, db) => {
            if (err) throw err;
            callback(db);
        })
    }
}
